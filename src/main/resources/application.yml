server:
  port: 2024
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SalesManagementSystem_db?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Oluwapelumi

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.gmail.com
    port: 465
    username: pelumijsh@gmail.com
    password: ioxa sqpc ztdc wqss

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
          ssl:
            enable: true